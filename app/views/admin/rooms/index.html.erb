<div class="container mt-3">
  <div class="row">
    <div class="col">

      <h1>Room一覧</h1>
      <ul>
        <% @rooms.each do |room| %>
          <li><%= link_to room.id, admin_room_path(room) %></li>
        <% end %>
      </ul>

    </div>
  </div>
</div>